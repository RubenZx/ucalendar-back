# Migration `20200725172147`

This migration has been generated by RubenZx at 7/25/2020, 5:21:47 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `ucalendar`.`SubjectGroup` DROP FOREIGN KEY `SubjectGroup_ibfk_1`

ALTER TABLE `ucalendar`.`SubjectGroup` DROP FOREIGN KEY `SubjectGroup_ibfk_2`

DROP TABLE `ucalendar`.`SubjectGroup`;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200725165702..20200725172147
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "mysql"
-  url = "***"
+  url = "***"
 }
 enum Role {
   ALUMN
@@ -38,18 +38,16 @@
 model Group {
   id            Int             @default(autoincrement()) @id
   name          String          @unique
-  subjects      SubjectGroup[]
   timeTableItem TimeTableItem[]
 }
 model Subject {
   id            Int             @id
   name          String
   abrev         String
   degrees       DegreeSubject[]
-  groups        SubjectGroup[]
   timeTableItem TimeTableItem[]
 }
 model TimeTableItem {
@@ -70,16 +68,8 @@
   subjectId    Int
   @@unique([classRoom, dayOfTheWeek, startHour, endHour], name: "classRoom")
 }
-model SubjectGroup {
-  group     Group   @relation(fields: [groupId], references: [id])
-  groupId   Int
-  subject   Subject @relation(fields: [subjectId], references: [id])
-  subjectId Int
-  @@id([groupId, subjectId])
-}
-
 model DegreeSubject {
   degree    Degree  @relation(fields: [degreeId], references: [id])
   degreeId  Int
   subject   Subject @relation(fields: [subjectId], references: [id])
```


